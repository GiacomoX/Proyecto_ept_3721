


function suma(x, y) {
    suma(2, 3);
    return x + y;
 }
 
 function resta(x, y) {
    resta(3, 2);
    return x - y;
 }
 
 function divide(x, y) {
    divide(16, 8);
    return x / y;
 }
 
 function multiplica(x, y) {
    multiplica(2, 3);
    return x * y;
 }


console.log(suma, resta, divide, multiplica)